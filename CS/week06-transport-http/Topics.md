#스터디/week6md


## WEEK 6 — 전송 계층 + HTTP + 실무 관점

### 📖 학습 키워드
#### **전송 계층**
1. TCP vs UDP
   * 연결 지향 vs 비연결
   * 신뢰성 vs 지연
   * Handshake 개념
   * Latency 관점
   * 재전송 존재 개념

#### **네트워크 실무 필수**
1. DNS
   * 도메인 → IP 변환 역할
2. HTTP 핵심
   * HTTP 특성
   * Stateless
   * 메시지 구조
   * 메서드
   * 상태 코드
   * 캐시 / 쿠키

#### **실무 연결 사고**
1. 성능 & 비용 관점
   * 왜 네트워크가 느려지는가
   * 네트워크 비용 vs CPU 비용
   * 왜 비동기가 기본 전략인지
   * 타임아웃 / 재시도 감각

### 📢 발표 주제
#### ① 전송 계층의 존재 이유

- IP의 한계
- 신뢰성 문제
- 왜 TCP가 등장했는가

#### ② TCP vs UDP 본질 비교

- 연결 지향 vs 비연결
- 신뢰성 vs 지연
- 사용 사례 차이

#### ③ TCP Handshake 메커니즘

- 연결 수립 과정
- 왜 필요한가
- 비용 발생 이유

#### ④ Latency 관점 사고

- 왜 네트워크가 느린가
- RTT 개념
- 사용자 경험 영향

#### ⑤ DNS의 역할

- 도메인 → IP 변환
- 왜 필요한가
- 실제 통신 흐름 연결

#### ⑥ HTTP 핵심 구조 & 특성

- Stateless
- 요청-응답 구조
- 메서드 / 상태 코드
- 캐시 / 쿠키
- 실무 영향

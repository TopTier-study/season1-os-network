name: Slack - Discussion Notify (styled)

on:
  discussion:
    types: [created]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Post discussion to Slack
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DISCUSSION_WEBHOOK }}

          REPO: ${{ github.repository }}
          TITLE: ${{ github.event.discussion.title }}
          URL: ${{ github.event.discussion.html_url }}
          AUTHOR: ${{ github.event.discussion.user.login }}
          CATEGORY: ${{ github.event.discussion.category.name }}
          BODY: ${{ github.event.discussion.body }}
          LABELS: ${{ join(github.event.discussion.labels.*.name, ', ') }}

        run: |
          set -e

          TARGET=$(printf "%s" "$BODY" | awk '
            BEGIN{target=""}
            /^##[[:space:]]*(질문 대상)/ {
              getline;
              gsub(/^- /,"");
              gsub(/^[[:space:]]+|[[:space:]]+$/,"");
              target=$0
            }
            END{print target}
          ')

          if [ -z "$TARGET" ]; then
            TARGET="지정 안 함 (전체)"
          fi

          # --- Slack 멘션 매핑
          mention=""
          case "$TARGET" in
            "지연") mention="<@U0AGAT6C15G>" ;;
            "윤서") mention="<@U0AGCV8GPJA>" ;;
            "선재") mention="<@U0AGTH6D96V>" ;;
            "상유") mention="<@U0AFZ6BK5GX>" ;;
            "성국") mention="<@U0AFZ6KK7L7>" ;;
            "귀로") mention="<@U0AGJ7XTTJQ>" ;;
            *) mention="" ;;
          esac

          # 라벨 태그처럼 보이게 가공
          if [ -n "$LABELS" ]; then
            LABEL_FORMATTED=$(echo "$LABELS" | sed 's/, /` `/g')
            LABEL_FORMATTED="`$LABEL_FORMATTED`"
          else
            LABEL_FORMATTED="`none`"
          fi

          if [ -n "$mention" ]; then
            header_text="📢 $mention *New Discussion Created!*"
          else
            header_text="📢 *New Discussion Created!*"
          fi

          payload=$(jq -n \
            --arg header "$header_text" \
            --arg repo "$REPO" \
            --arg title "$TITLE" \
            --arg url "$URL" \
            --arg author "$AUTHOR" \
            --arg category "$CATEGORY" \
            --arg target "$TARGET" \
            --arg labels "$LABEL_FORMATTED" \
            '{
              attachments: [
                {
                  color: "#9B59B6",
                  blocks: [
                    {
                      type: "section",
                      text: { type: "mrkdwn", text: $header }
                    },
                    { type: "divider" },
                    {
                      type: "section",
                      text: { type: "mrkdwn", text: "*<" + $url + "|" + $title + ">*\n📦 _" + $repo + "_" }
                    },
                    {
                      type: "section",
                      fields: [
                        { type: "mrkdwn", text: "🗂 *Category*\n" + $category },
                        { type: "mrkdwn", text: "👤 *Author*\n" + $author },
                        { type: "mrkdwn", text: "🎯 *Target*\n" + $target },
                        { type: "mrkdwn", text: "🏷 *Labels*\n" + $labels }
                      ]
                    },
                    {
                      type: "actions",
                      elements: [
                        {
                          type: "button",
                          text: { type: "plain_text", text: "🔗 Open Discussion" },
                          url: $url
                        }
                      ]
                    }
                  ]
                }
              ]
            }'
          )

          curl -X POST -H 'Content-type: application/json' --data "$payload" "$SLACK_WEBHOOK_URL"
